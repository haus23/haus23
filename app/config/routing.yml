app:
    resource: "@AppBundle/Controller/"
    type:     annotation

logout:
    path: /logout

dtp_standings:
    resource: "@AppBundle/Controller/Dtp/Standings/"
    type:     annotation

dtp_backend:
    resource: "@AppBundle/Controller/Dtp/Backend/"
    type:     annotation
    prefix:   'tipprunde/admin'
